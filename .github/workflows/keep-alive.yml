name: Keep LocalMind Warm

on:
  schedule:
    - cron: '0 */12 * * *'  # Every 12 hours at minute 0
  workflow_dispatch:       # Optional: allow manual trigger

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Curl LocalMind
        run: curl -s https://harshkumarsaw-localmind.hf.space/ > /dev/null
